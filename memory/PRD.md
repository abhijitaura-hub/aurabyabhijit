# AURA BY ABHIJIT — PRD

## Original Problem Statement
Build a world-class personal technology authority website for Abhijit Debnath (aurabyabhijit.com): "Technology Leadership for an Intelligent Future." A personal leadership/advisory/thought-leadership platform — NOT a generic IT services site. Strict truth rules: only verified facts (20+ yrs IT, 10+ yrs leadership, 65+ distributed locations, enterprise/cloud/Azure/security/transformation/governance/automation/AI). Never fabricate awards, clients, testimonials, stats, or URLs. Premium Dark Editorial Technology design system; Sora headings / Inter body; obsidian + electric cyan + subtle violet; WCAG 2.2 AA; SEO foundation; future-proof content architecture.

## Architecture
- Frontend: React 19 + Tailwind (brand palette: red #ff2e3e "crimson" + black/white, per Abhijit's logo), framer-motion (reveals, masked line hero), lenis (smooth scroll), react-router-dom (11 routes), react-helmet-async (SEO + JSON-LD Person/WebSite/Article schema). Content separated in src/data/site.js. Official AURA logo + real portrait integrated (hero frame, home teaser, about page, navbar mark, footer lockup, favicons).
- Backend: FastAPI + MongoDB (motor). /api/articles (filter/search/pagination), /api/articles/{slug} (+related), /api/contact (validation + honeypot), /api/admin/login (bcrypt + JWT + brute-force lockout), /api/admin/messages (Bearer).
- Ops: robots.txt, sitemap.xml, Google Fonts preconnect, film-grain overlay, blueprint grid, prefers-reduced-motion respected.

## User Personas
CEO/CIO/CTO evaluating credibility; advisory clients; recruiters; YouTube/LinkedIn followers; first-time visitors (5-second test).

## Implemented (2026-08-31)
- Home: kinetic masked-line hero + abstract "AD" monogram portrait frame w/ canvas node network + mouse parallax; credibility strip (20+/10+/65+/Enterprise); editorial marquee; Six Dimensions interactive cards; Philosophy manifesto with contrast pairs; From the Field tabbed experience cards (Problem→Thinking→Approach→Outcome); AURA Decision Framework scroll-progress timeline; Perspective preview; About teaser; Speaking strip; Final CTA.
- Pages: About (story + verified experience + career timeline + principles), Expertise (6 dimensions deep-dive + framework + advisory areas), Perspective (featured + filters + search), Article detail (breadcrumbs, share, related, schema), Projects (honest "coming soon" + case-study structure), Speaking (topics + YouTube/LinkedIn placeholders + Invite Abhijit), Work With Me (6 advisory areas + engagement steps), Contact (validated form → MongoDB), Privacy & Terms (editable placeholders, marked for legal review), Admin inbox, 404.
- Backend: 4 seeded draft articles (marked "Draft — awaiting review"), contact storage, admin JWT auth with lockout.
- Verified: health/articles/article/contact/admin endpoints via curl; hero, framework, filters, article, contact success, mobile hero + menu via screenshots; no console errors.

## Backlog / Next
- P0: Abhijit reviews/edits the 4 draft articles; configure LinkedIn/YouTube URLs (SOCIALS in site.js). DONE 2026-08-31: official AURA logo integrated (navbar mark, footer full lockup, favicons) + Abhijit's real portrait integrated (hero frame, home about teaser, about page) with identity-safe treatment only (crop, grade, edge vignette).
- P1: case studies content. DONE 2026-08-31: Article Studio live in /admin (write/edit/publish/delete articles, draft vs published, featured flag, SEO fields, heading/quote/paragraph blocks, auto reading time, auto slugs) + hero image uploads via Emergent object storage (POST /api/admin/upload auth-guarded, public GET /api/media/{path}, covers render on cards + article pages). Newsletter capture live at end of articles (POST /api/newsletter/subscribe, honeypot, dedupe) with subscriber list in admin inbox tab. OG/Twitter share images wired: article hero image when present, portrait fallback otherwise. DONE 2026-09-01: custom domain aurabyabhijit.com + www live with SSL (Entri/Cloudflare flow; BigRock DNS). Real socials connected (LinkedIn /in/ad-abhi/, YouTube @aurabyabhijit, Facebook). Transactional email via managed Resend: contact-form owner notification + newsletter welcome (OWNER_EMAIL=abhijit.aura@gmail.com). Redeploy triggered to ship these to production. Privacy-friendly analytics added 2026-09-01: POST /api/analytics/track (no IPs, no cookies, /admin excluded), GET /api/admin/analytics (total/by page/daily/referrers, 7/30/90d ranges), Analytics tab in /admin. Site Settings feature: GET /api/settings (public) + PUT /api/admin/settings (auth) for phone, public email, LinkedIn/YouTube/Facebook — runtime SettingsProvider overrides frontend defaults live, Contact page shows phone/email when set. Google verification meta tag embedded in index.html. Tier-1+3 self-editing 2026-09-01: settings content supports speaking_topics/timeline/advisory_areas/dimensions/field_notes (all consumed with safe defaults); change-password endpoint (password_source=manual survives reseeds); inbox read/delete; draft preview links (?preview=1 + admin token) for articles & recommendations. Theme switcher 2026-09-05: Dark/Light/System via ThemeProvider (localStorage aura-theme, prefers-color-scheme for system), navbar toggle (Moon/Sun/Monitor cycle), light palette as html.light CSS overrides in index.css. GA4 via REACT_APP_GA_ID env. AURA AI chatbot 2026-09-14: POST /api/chat (SSE stream, gemini-2.5-flash via Emergent key, 20 req/hr per IP, 500-char input cap, last-6 history, max_tokens 1200 incl. thinking, graceful fallback on balance exhaustion — no auto top-up), AuraChat.jsx floating widget bottom-left (WhatsApp owns bottom-right), hidden on /admin, light-theme aware. AURA Recommendations added 2026-09-01 (purely additive): db.recommendations + db.recommendation_categories (9 seeded), editorial workflow (draft→review→approved→published→update_required→archived, only published is public), AURA Score 1.0–10.0 + 5 optional sub-scores, badges (AURA PICK/VALUE/PRO), merchants with affiliate URLs + per-program disclosure override, outbound click events via /api/analytics/track (event+target fields, pageviews still filtered separately). Routes: /recommendations, /recommendations/:category (9 seeded incl. aura-picks auto-populated by badge and my-setup grouped by setup_group), /recommendations/:category/:slug detail, /affiliate-disclosure (editable via Settings disclosure_text). Nav item added after Perspective; homepage preview section inserted directly after hero (4 featured categories); footer has Affiliate Disclosure link. Admin → Recommendations tab with full editor + category creation. No fabricated products seeded — empty states say "Evaluating". Personal-voice content refresh 2026-09-14 (content-only, no design changes): hero subcopy now first-person ("More than 20 years in technology has taught me…", default in DEFAULT_CONTENT.hero_subcopy, settings-driven, DB untouched); hero primary button "Explore AURA" → "Ask AURA" (opens existing chatbot via window event 'aura:open-chat', listener added in AuraChat.jsx — no chatbot architecture/model/API/limits/disclaimer changes); three new homepage sections after Hero: WhyIBuiltAura, AskAura (button opens same chatbot), WhatIShare (6 experience areas); footer disclaimer replaced with personal independence statement verbatim; html overflow-x:hidden added (fixes pre-existing ~12px mobile overflow, zero visual change). Verified: testing agent 100% pass (desktop/mobile/light, chat streams via /api/chat, no console errors). DEPLOYED to aurabyabhijit.com 2026-09-14 (bundle main.ce068462.js, verified live: new subcopy, Ask AURA hero button opens chat, Why I Built AURA section live). Final homepage refinements 2026-09-14: rotating hero word (TaglineLoop) fully removed incl. dead code/imports — hero paragraph starts directly with the first-person copy; the three new sections' vertical padding reduced py-24/md:py-32 → py-16/md:py-20. Verified desktop+mobile (0px overflow, chat streams, nav/Work With Me intact). DEPLOYED to aurabyabhijit.com 2026-09-14 (bundle main.2b0ad084.js, verified live: rotating word gone, subcopy starts naturally, chat opens). AURA AI mentor upgrade 2026-09-14 (approved scope, no architecture change): CHAT_SYSTEM rewritten from "navigation assistant" (2-4 sentences, guide-to-pages) to technology-mentor behavior — understand goal → practical answer → next best step → offer roadmap only for goal-oriented questions ("Would you like me to create a roadmap for you?") → max one follow-up question → max one natural content pointer; roadmap structure = current situation/target/key areas/priorities/next steps/30-60-90/content; plain-text formatting only; ALL guardrails preserved verbatim (verified facts, employer/privacy boundary, no fabrication, /work-with-me routing). Fixed latent SSE framing bug exposed by multi-line answers: tokens containing "\n\n" broke the frontend parser and silently dropped reply text — backend now escapes newlines in tokens (event.content.replace), frontend unescapes after [DONE] check. Bubble got whitespace-pre-wrap + post-stream cleanup stripping stray ** / ## artifacts. Unchanged: gemini-2.5-flash, EMERGENT_LLM_KEY, max_tokens=1200, 20/hr rate limit, 500-char input, history handling, disclaimer, WhatsApp, GA. Tested: CIO→offer→"yes"→complete roadmap (no truncation), simple Q→simple answer, Azure→decision factors+one follow-up, privacy probe→deflection, desktop+mobile render (0px overflow). DEPLOYED to aurabyabhijit.com 2026-09-14 (bundle main.4a4e07ac.js; verified live: CIO question ends with roadmap offer, IT-team question gives practical mentor answer with priorities/WIP/define-done, line breaks + newline escaping live, no stray markdown). Chat scroll-chaining fix 2026-09-14 (AuraChat.jsx only): root cause = global Lenis smooth-scroll + native scroll chaining — wheel/touch over the chat panel scrolled the page behind; data-lenis-prevent alone wasn't enough because Lenis bows out WITHOUT preventDefault, letting native scrolling chain to the document over the panel's non-scrollable parts. Fix: (1) data-lenis-prevent on panel root (Lenis ignores wheel/touch inside), (2) overscroll-contain on messages container (stops boundary chaining), (3) panel-scoped non-passive wheel+touchmove listeners preventDefaulting only when target is outside the messages container (header/footer/input), attached via panelRef while open. No body lock, no visual change, streaming/history/prompt untouched. Verified: desktop wheel inside (chat scrolls, page static), header/input wheels (page static), boundary past-scroll (no chain), mid-stream wheel (page static, stream completes), outside-scroll (page scrolls), close→normal; mobile CDP touch: inside (chat scrolls only), header (nothing scrolls), outside (page scrolls). Preview only until next deploy. Hero portrait replacement 2026-09-14 (portrait-only, no redesign): user supplied new office portrait (navy blazer, dark office bg, 1145x1374 PNG). Stored untouched master at public/assets/portrait-hero.png (1.9MB) + visually-identical re-encode public/assets/portrait-hero.webp (150KB, served for speed — same precedent as earlier webp assets). HeroVisual.jsx only code change: motion.img src portrait-cutout.png → portrait-hero.webp; mask changed from bottom-only linear fade to radial-gradient(115% 88% at 50% 42%, black 62%, transparent 97%) so the rectangular photo feathers on all edges (no visible image box); subtle textShadow added to the "Abhijit Debnath" and "EST. 20+ YRS" overlay labels for legibility over the photo's brighter areas. Layout, chips, rim-light, parallax, filter grade, captions, positions all unchanged. About page + home About teaser intentionally still use portrait-cutout.png (hero-only scope). Admin-editable hero portrait 2026-09-14: hero_portrait added to site_settings (SettingsInput + clean_settings validation — must be an uploads media path, external URLs/traversal rejected 400; GET /api/settings exposes it publicly). Reuses existing POST /api/admin/upload (auth, JPG/PNG/WebP/GIF ≤8MB, Emergent object storage — survives redeploys) + GET /api/media/{path}. SettingsPanel: "Hero Portrait Image" block atop Site content (current preview, Replace Image file input w/ client type+size validation, staged indicator, Save uploads then PUTs settings). HeroVisual: src = hero_portrait ? mediaUrl(hero_portrait) : /assets/portrait-hero.webp fallback (no hardcode). settings.jsx exposes hero_portrait. Tested 100% (pytest backend/tests/test_hero_portrait.py — 6 tests incl. auth/validation; E2E admin login→upload→save→homepage shows uploaded image→persists on refresh; desktop/tablet/mobile; chatbot intact). Preview DB reset to null so fallback (office portrait) shows. DEPLOYED 2026-09-14 (bundle main.005e79a9.js; verified live: office portrait rendering via fallback, prod settings intact (hero_portrait null, phone/socials preserved), admin login OK, external-URL hero_portrait rejected 400, unauthenticated upload rejected 401). Admin/CMS audit delivered 2026-09-14 (report only, no code changes): full field+media inventory with A–F status codes, priority classification and proposed admin structure; key findings = admin "Site tagline" not wired to SEO title, admin "Site description (SEO default)" has no frontend consumer, About portrait still hardcoded (/assets/portrait-cutout.png in About.jsx + Home.jsx teaser), homepage personal sections (Why I Built AURA / Ask AURA / What I Share) hardcoded in Home.jsx, Projects has no CMS. Website Content admin 2026-09-14 (owner-approved HIGH items only): new Admin tab "Website Content" (ContentPanel.jsx) — Homepage group (hero_eyebrow, why_aura_title/p1/p2, ask_aura_title/text/button, share_items 6), AURA Chat group (3 suggestion chips), About group (about_portrait upload + bio_paragraphs with **bold**/*italic* markers), Contact group (contact_topics). Backend: clean_settings whitelists the new keys + top-level about_portrait (uploads-path validation like hero_portrait). Consumers: Hero.jsx eyebrow, Home.jsx 3 sections + AboutTeaser portrait, About.jsx bio (tiny marker parser, identical visual output) + portrait, AuraChat.jsx chips, Contact.jsx topics. SEO disconnect fix: SEO.jsx defaults now use content.tagline/content.description (admin tagline/description finally live). Data safety: both panels merge over raw fetched content and echo all top-level fields; ContentPanel saves only values differing from DEFAULT_CONTENT (no-change save = nothing stored). Tested 100% (pytest 7/7 backend/tests/test_content_editability.py + full E2E with revert-after-each-test; cross-tab wipe safety verified; mobile + console clean). DEPLOYED 2026-09-14 (bundle main.fa8fc958.js; verified live: Website Content tab present + prefilled, homepage pixel-identical, admin login OK, about_portrait validation 400, unauth rejected). Post-deploy fix: production content.tagline held eyebrow text which the newly-wired SEO title exposed; corrected to "Technology Leadership for an Intelligent Future." via full-echo PUT (all settings/content preserved; footer now reads correctly too). Removed a stray default-copy bio_paragraphs key from production content (exact defaults; code defaults own it now). Focused content fixes 2026-09-15: (1) "Draft — awaiting review" badge removed from public ArticleCard + ArticlePage (public API already served published-only; publish now also clears is_draft_content on save); (2) ask_aura_support default updated to "...cybersecurity, automation or your next career move — and get practical guidance based on Abhijit's experience and ideas."; (3) stat changed to "77+ Locations Enabled" + optional stat note field ("Experience supporting technology across distributed business operations.") — note added to site.js STATS, Stats.jsx render, backend clean_stats, SettingsPanel stats editor, and written to BOTH preview + production DB stats[2]; (4) Recommendations framing now personal: page H1 "Tools and technologies I would recommend to other technology leaders." + honest-take paragraph; homepage preview lede = honest-take line. IMPORTANT: production save of the stat note was stripped by the OLD production backend — after the next deploy, re-save production stats so the note lands. Verified preview: all four render correctly, no draft badges anywhere, published-only feed, no layout breakage. Ask AURA supporting line 2026-09-15: new settings key ask_aura_support added (backend whitelist + DEFAULT_CONTENT + ContentPanel field "Ask AURA — supporting line"). Home.jsx AskAura: SectionHead lede now renders ask_aura_support ("Ask AURA about technology leadership, AI, cloud, security or your next career move — and get practical, experience-based guidance.") directly under the heading; existing invitation line (ask_aura_text) kept as a second paragraph (mt-3, same lede styling, data-testid=ask-aura-invite). Button/behavior/architecture untouched. Before/after screenshotted desktop+mobile, 0px overflow, chat opens. Preview only until next deploy. GA4 + GSC setup 2026-09-15: verified GA4 (G-R0X3V7HK8K via REACT_APP_GA_ID) fires on every SPA route incl. /recommendations/ai-tools; App.js route-change page_view now also sends page_title + page_location. New dynamic sitemap GET /api/sitemap.xml (12 static pages + published articles + rec categories + published items; SITE_URL constant = aurabyabhijit.com) + robots.txt now lists both sitemaps. GSC HTML verification file confirmed live at /google72b35cf05d135a09.html. Deployed (verified live: sitemap 200, 28 URLs incl. 4 published articles + 3 published recs, XML valid, robots updated). New-vs-returning and Path exploration are GA4-native (no code needed); UTM attribution automatic via gtag landing capture. USER MANUAL STEPS REMAINING: click Verify in GSC + submit sitemap(s). GSC verification fix 2026-09-15: user's console expected a NEW token file googlee9c9c0085f397914.html (site only had the older google72b35cf05d135a09.html — hence "wrong content" error). Placed the uploaded file in frontend/public/ and kept the old one (Google: never remove verification files). Gotcha learned: files created via raw bash are NOT auto-committed — the first deploy missed it (SPA fallback served index.html); fixed by git add+commit then redeploy. Verified live: BOTH verification files serve exact token content with 200. Starter chips personalized 2026-09-15 (production content.chat_suggestions): "I want to become a CIO. What should I do?" / "How do I introduce AI into my business?" / "How do I get my IT team to deliver more?" — verified live: chips render, click sends, mentor reply with roadmap offer. All other settings/data preserved. About Photo Sync 2026-09-15: office portrait set as live About Profile Image (about_portrait = aura/uploads/articles/8f4fa170-…webp in production; preview has its own copy). About.jsx + Home.jsx teaser now apply the radial feather mask only when a custom about_portrait is set — fallback cutout rendering 100% unchanged (deployed as bundle main.c083b3e7.js). Verified live: About page + homepage teaser both show the office portrait cleanly blended, bio formatting intact, user data (77+ stats, tagline) preserved. Note: production content.bio_paragraphs currently stores an exact copy of defaults (re-appeared during the day, likely an admin save while exploring; harmless — renders identically). chatbot collapsed by default (pill toggle bottom-left), panel resized to 350px wide / h-[min(500px,100dvh-110px)] so it never covers hero text, welcome message + suggestions prioritized, disclaimer restored as one-line micro-text (8px mono, uppercase) under header per user decision, close button enlarged to 36px tap target, mobile width calc(100vw-32px). Verified across desktop/mobile × dark/light via screenshots; deployed to aurabyabhijit.com.
- P2: Newsletter signup; videos/podcasts/courses sections (architecture ready); CMS migration; Lighthouse/perf audit pass; sitemap auto-generation including articles.
- UTM verification 2026-09-15: loaded aurabyabhijit.com/?utm_source=linkedin&utm_medium=social&utm_campaign=first_post and confirmed the GA4 collect call carries the full tagged URL in `dl` (GA4 derives source/medium/campaign from it server-side — cs/cm/cn absent is normal for gtag.js). No code needed; UTM tracking works. User's manual step: share the tagged link on LinkedIn; results appear under Reports → Acquisition → Traffic acquisition → Session source/medium.
- GA4 key events 2026-09-15: new trackGaEvent(name, params) helper in lib/api.js (window.gtag wrapper, skips navigator.webdriver like trackPageview — note: api.js ALREADY had a different trackEvent(path,event,target) for self-hosted affiliate analytics; kept separate, untouched). Wired: work_with_me_click{location: hero|navbar|navbar_mobile} on all three Work With Me CTAs; booking_click{location: contact|work_with_me|final_cta} on all Book-a-Call links; generate_lead{topic} on successful contact submit (GA4 recommended event name). Files: lib/api.js, Navbar.jsx, home/Hero.jsx, Home.jsx, WorkWithMe.jsx, Contact.jsx. Verified via dataLayer on preview (work_with_me_click{hero} and generate_lead{topic:AI Strategy} confirmed queued to gtag; form success intact). Test submissions went to PREVIEW inbox only (ga4test@example.com ×2 — can delete). USER GA4 STEP: GA4 → Admin → Events → mark generate_lead (and optionally work_with_me_click, booking_click) as Key events once they appear (~24h). Preview only until next deploy.
- DEPLOYED 2026-09-15 (bundle main.69775af7.js): GA4 key events + Ask AURA support line + 77+ stat note + draft-badge removal + Recommendations framing all live. Verified on production: stat note renders, Ask AURA copy live, reco framing live, Perspective shows zero draft badges (user's real published articles visible), work_with_me_click{hero} confirmed in GA4 dataLayer on prod. Production stats note re-saved successfully (new backend preserves it). Note: GA4 mark-as-key-event toggle remains the user's manual step (Admin → Events, ~24h after first real events).
